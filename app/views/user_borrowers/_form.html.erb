<%= form_for(@user_borrower) do |f| %>
  <% if @user_borrower.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_borrower.errors.count, "error") %> prohibited this user_borrower from being saved:</h2>

      <ul>
      <% @user_borrower.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :registered_date %><br>
    <%= f.date_select :registered_date %>
  </div>
  <div class="field">
    <%= f.label :status %><br>
    <%= f.number_field :status %>
  </div>
  <div class="field">
    <%= f.label :identity_type_id %><br>
    <%= f.collection_select :identity_type_id, IdentityType.all, :id, :description, :selected => @user_borrower.identity_type %>
  </div>
  <div class="field">
    <%= f.label :identity_no %><br>
    <%= f.text_field :identity_no %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
